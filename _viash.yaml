name: rnaseq
version: v0.2.0

viash_version: 0.9.0

source: src
target: target

info:
  test_resources:
    - path: gs://viash-hub-test-data/rnaseq/v1
      dest: testData

config_mods: |
  .requirements.commands := ['ps']
  .resources += {path: '/src/workflows/utils/labels.config', dest: 'nextflow_labels.config'}
  .runners[.type == 'nextflow'].directives.tag := '$id'
  .runners[.type == 'nextflow'].config.script := 'includeConfig("nextflow_labels.config")'

repositories:
  - name: biobox
    type: git
    uri: https://packages.viash-hub.com/vsh/biobox.git
    tag: main
  - name: craftbox
    type: git
    uri: https://packages.viash-hub.com/vsh/craftbox.git
    tag: v0.1.0
