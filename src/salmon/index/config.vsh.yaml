functionality:
  name: salmon_index
  namespace: salmon
  description: | 
    Create Salmon index.
  
  arguments: 
    - name: "--genome_fasta"
      type: file
    - name: "--transcriptome_fasta"
      type: file

    - name: "--salmon_index"
      type: file
      direction: output

  resources:
    - type: bash_script
      path: script.sh
  test_resources:
    - type: bash_script
      path: test.sh

platforms:
  - type: nextflow
#   - type: docker
#     image: ubuntu:20.04
#     setup:
#       - type: docker
#         run: |
#           apt-get update && \
#           apt-get install -y --no-install-recommends git gcc make g++ libboost-all-dev liblzma-dev libbz2-dev ca-certificates zlib1g-dev libcurl4-openssl-dev tar ca-certificates gnupg software-properties-common wget && \
#           apt-get clean && \
#           wget --no-check-certificate https://github.com/COMBINE-lab/salmon/releases/download/v1.10.0/salmon-1.10.0_linux_x86_64.tar.gz && \
#           tar xzf salmon-1.10.0_linux_x86_64.tar.gz && \
#           cp salmon-latest_linux_x86_64/bin/salmon /usr/local/bin/

        