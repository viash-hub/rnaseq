#!/bin/bash

OUTPUT_DIR="resources_test/some_fastq"

# all of the commands needed to generate `resources_test/some_fastq`

# we make sure that the name of the script (i.e. src/resources_test_scripts/some_fastq.sh) corresponds to the output dir.
